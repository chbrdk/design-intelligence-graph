# Local HTML demos

**Date:** 2026-08-18  
**Folder:** `tmp/` (gitignored)  
**Rule:** `.cursor/rules/local-html-demos.mdc`

Example pages from MCP / craft briefs are **one local HTML file**, opened directly. Not an island route, not `paths.ts`, not a Coolify deploy — unless we are asked to ship the page into SPIRION.

Current example: `tmp/northline.html` (City Arcade craft → Northline insurance). Image: `tmp/northline-architecture.png`.
